// Object
var animal = {}

const person = {
    name: 'John',
    age: 30,
    weitgh: 80,
    isAdmin: true
}

console.log(person);

// Acessando os atributos do objeto
console.log(person.name);
console.log(`${person.name} tem ${person.age} anos`);
